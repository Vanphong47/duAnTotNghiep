mixin filterState(filterState)
    each item in filterState
        button.border(
            class=`p-[10px] rounded-[8px] mr-[10px] ${item.class} hover:bg-[#1876D2]`
            button-status = item.status
            ) #{item.name}